services:
  - type: web
    name: bias-detection-dashboard
    runtime: python
    rootDir: automatic_bias_detection_dashboard
    buildCommand: pip install -e .
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.11"
